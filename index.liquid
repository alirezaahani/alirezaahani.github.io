<!DOCTYPE html>
<html lang="en">
  <head>
    {% include "head.liquid" %}
  </head>
  <body>
    <header>
      {% include "header.liquid" %}
    </header>
    <main>
      <section>
        Blog posts: 
        <ol reversed>
         {% for post in collections.posts.pages %}
          <li>
            <a href="{{ post.permalink }}">{{ post.title }}</a>
            <time pubdate="pubdate"> - {{ post.published_date | date: "%Y/%m/%d" }}</time>
            {% if post.categories.size > 0 %}<span>- {{ post.categories | join: ", " }}</span> {% endif %}
          </li>
          {% endfor %}
        </ol>
      </section>

      Contact: 
        <ul>
          <li>Email: alirezaahani1386 [at] gmail [dot] com</li>
          <li>Telegram: @alireza_ahani</li>
        </ul>
    </main>
    <footer>
      {% include "footer.liquid" %}
    </footer>
  </body>
</html>
